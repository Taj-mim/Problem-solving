#include<stdio.h>
double area(double base,double height)
{
    return 0.5*base*height;
}
int main()
{
    double area(x,y),x,y;
    scanf("%lf",&x);
    scanf("%lf",&y);
    printf("%lf",area(x,y));
}
