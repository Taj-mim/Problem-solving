#include<stdio.h>
int main()
{
    char name[ ]={"my name fatema"};
    /*
    int len=0,i=0;
    while(name[i]!='\0')
    {
        i++;
        len++;
    }

    printf("%d",len);*/
    printf("%d",strlen(name));
}
