#include<iostream>
#include<cmath>
int main()
{
    int x,y,z;
    using std::cin,std::cout;
    cin>>x;
    cin>>y;
    /*z=std::max(x,y);
    cout<<z;*/
    // cout<<std::max(x,y);
    //cout<<std::min(x,y);
    //cout<<"the root of x="<<sqrt(x);
   // cout<<pow(3,2);
   cout<<ceil(3.44);
    return 0;


}
